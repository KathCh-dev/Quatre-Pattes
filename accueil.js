// Questions du Quizz:

// // Les associations gardent la majorité des dons pour leur fonctionnement
// Mauvaise réponse : "Vrai, les associations utilisent plus de 50% des dons pour leurs frais de fonctionnement"
// Bonne réponse : "Faux ! Chez Quatre Pattes, 85% des dons sont directement utilisés pour les animaux. Seuls 15% servent aux frais de fonctionnement essentiels."
// Les refuges ne prennent que les animaux en bonne santé
// Mauvaise réponse : "Vrai, ils refusent les animaux malades car c'est trop coûteux"
// Bonne réponse : "Faux ! Nous accueillons tous les animaux, quel que soit leur état de santé. Chaque
// Les associations ont trop de moyens grâce aux dons
// Mauvaise réponse : "Vrai, elles reçoivent beaucoup d'argent des donateurs"
// Bonne réponse: "Faux ! Les besoins sont immenses et constants. Chaque don est précieux pour sauver plus d'animaux."

// Section 2: l'encart de don
// pseudo-code: 
// Quand l'user clique sur un montant, le montant s'affiche dans le bouton de confirmation sous la forme "faire un don de nn€" 
// Quand l'user clique sur une périodicité, la périodicité s'affiche dans le boutopn de confirmation sous la forme "faire un don de ... /périodicité" 
// Quand le bouton de montant est sélectionné, il a un contour vert et un fond vert clair. 
// Quand le bouton de périodicité est sélectionné, il a un fond vert et une écriture blanche.


// function amountSelect(userAmount, userFrequency){
//     let userAmount = document.getElementsByClassName('amount-buttons').value;
//     let userFrequency = document.getElementsByClassName('frequency-buttons').value;

//     let tenAmount = document.getElementById('ten-amount')
//     let twentyAmount = document.getElementById('twenty-amount')
//     let fiftyAmount = document.getElementById('fifty-amount')
//     let validationButton = document.getElementById('donation-validation-button')


//     if(userAmount === tenAmount){
//         validationButton.innerHTML = "Faire un don de " + tenAmount.innerHTML
//     }else if(userAmount === twentyAmount){
//         validationButton.innerHTML = "faire un don de " + twentyAmount.innerHTML
//     }else if(userAmount === fiftyAmount){
//         validationButton.innerHTML = "faire un don de " + fiftyAmount.innerHTML
//     }

//     if(userFrequency = once){
//         validationButton.innerHTML = validationButton.innerHTML + " une fois"
//     }else if(userFrequency = monthly){
//         validationButton.innerHTML = validationButton.innerHTML + " / mois"
//     }else if(userFrequency = annual){
//         validationButton.innerHTML = validationButton.innerHTML + " / an"
//     }

// }








// Section 4: histoires de réussites
// pseudo-code:
// Quand le user clique sur une des images miniatures, l'image principale change pour l'image sélectionnée et le texte de l'image principale change pour le texte lié à l'image sélectionnée.









// Section 5: testez vos connaissances
// pseudo-code:
// Quand l'user clique sur une réponse, elle apparaît en fond vert clair avec une coche verte sur la gauche du texte lorsque c'est la bonne réponse; 
// elle apparaît en fond rouge clair avec une croix rouge sur la gauche du texte lorsque c'est la mauvaise réponse. 
// Il ne doit rester qu'un seul espace réponse quand l'user clique sur une réponse. 
// Quand le quizz est complété, le score doit apparaître en dessous de la section quizz, au dessus de la section impact.

